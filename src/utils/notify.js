// Notification Utility Stub
export async function sendNotification({ to, subject, message }) {
  // TODO: Integrate with email/SMS/Push provider
  console.log(`Notify ${to}: ${subject} - ${message}`);
}
